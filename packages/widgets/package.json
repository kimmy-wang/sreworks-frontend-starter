{
  "name": "@sreworks/widgets",
  "version": "0.0.0-alpha.1",
  "description": "",
  "keywords": [],
  "license": "Apache-2.0",
  "author": "",
  "main": "lib/index.js",
  "module": "es/index.js",
  "scripts": {
    "build": "npm-run-all -p build:*",
    "build:cjs": "rm -rf lib/* && babel src -d lib --config-file ./.babelrc.cjs.js --no-babelrc",
    "build:es": "rm -rf es/* && babel src -d es --config-file ./.babelrc.es.js --no-babelrc",
    "build:umd": "rm -rf dist/* && babel src -d dist"
  },
  "dependencies": {
    "@sreworks/shared": "^0.0.0-alpha.1",
    "react": "17.0.2"
  },
  "publishConfig": {
    "access": "public"
  }
}
